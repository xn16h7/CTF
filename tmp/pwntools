from pwn import *

HOST = ""
PORT = 
conn= ptgrocess("./")
#conn = remote(HOST, PORT)

ret = conn.recv() #出力をEOFまで
ret = conn.recvuntil(':') #出力を:まで
ret = p.recvline() # 改行まで
ret = p.recvline(timeout=0.01) # timeout

log.info(ret) 

payload = ""
payload += p32(shell)

conn.sendline()　#shellcode


conn.interactive()
