from pwn import *

HOST = ""
PORT = 
conn= ptgrocess("./")
#conn = remote(HOST, PORT)

ret = conn.recv() #出力をEOFまで
#ret = conn.recvuntil('') #出力を引数の文字列まで

log.info(ret) 

payload = ""
payload += p32(shell)

conn.sendline()　#shellcode


conn.interactive()
